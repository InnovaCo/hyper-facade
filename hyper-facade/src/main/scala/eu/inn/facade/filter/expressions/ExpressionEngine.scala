package eu.inn.facade.filter.expressions

import eu.inn.facade.model.FacadeRequestContext

object ExpressionEvaluator {

  def evaluate(ifExpression: String, context: FacadeRequestContext): Boolean = {
    true
  }
}
